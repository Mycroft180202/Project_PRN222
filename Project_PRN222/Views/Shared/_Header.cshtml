<!-- Topbar Start -->
<div class="container-fluid">
    <div class="row bg-secondary py-2 px-xl-5">
        <div class="col-lg-6 d-none d-lg-block">
            <div class="d-inline-flex align-items-center">
                <a class="text-dark" href="@Url.Action("Index", "Home")#faq">FAQs</a>
                <span class="text-muted px-2">|</span>
                <a class="text-dark" href="@Url.Action("Contact", "Home")">Help</a>
                <span class="text-muted px-2">|</span>
                <a class="text-dark" href="@Url.Action("Contact", "Home")">Support</a>
            </div>
        </div>
        <div class="col-lg-6 text-center text-lg-right">
            <div class="d-inline-flex align-items-center">
                <a class="text-dark px-2" href="#"><i class="fab fa-facebook-f"></i></a>
                <a class="text-dark px-2" href="#"><i class="fab fa-twitter"></i></a>
                <a class="text-dark px-2" href="#"><i class="fab fa-linkedin-in"></i></a>
                <a class="text-dark px-2" href="#"><i class="fab fa-instagram"></i></a>
                <a class="text-dark pl-2" href="#"><i class="fab fa-youtube"></i></a>
            </div>
        </div>
    </div>
    <div class="row align-items-center py-3 px-xl-5">
        <div class="col-lg-3 d-none d-lg-block">
            <a href="@Url.Action("Index", "Home")" class="text-decoration-none">
                <h1 class="m-0 display-5 font-weight-semi-bold"><span class="text-primary font-weight-bold border px-3 mr-1">E</span>Shopper</h1>
            </a>
        </div>
        <div class="col-lg-6 col-6 text-left">
            <form asp-action="Search" asp-controller="Home" method="get">
                <div class="input-group">
                    <input type="text" class="form-control" name="query" placeholder="Search for products">
                    <div class="input-group-append">
                        <button type="submit" class="input-group-text bg-transparent text-primary">
                            <i class="fa fa-search"></i>
                        </button>
                    </div>
                </div>
            </form>
        </div>
        <div class="col-lg-3 col-6 text-right">
            <a href="/Cart/Cart" class="btn border" id="cartButton">
                <i class="fas fa-shopping-cart text-primary"></i>
                <span class="badge" id="cartBadge">0</span>
            </a>
        </div>
    </div>
</div>
<!-- Topbar End -->
<!-- Navbar Start -->
<div class="container-fluid mb-5">
    <div class="row border-top px-xl-5">
        <div class="col-lg-3 d-none d-lg-block">
            <a class="btn shadow-none d-flex align-items-center justify-content-between bg-primary text-white w-100" data-toggle="collapse" href="#navbar-vertical" style="height: 65px; margin-top: -1px; padding: 0 30px;">
                <h6 class="m-0">Categories</h6>
                <i class="fa fa-angle-down text-dark"></i>
            </a>
            <nav class="collapse position-absolute navbar navbar-vertical navbar-light align-items-start p-0 border border-top-0 border-bottom-0 bg-light" id="navbar-vertical" style="width: calc(100% - 30px); z-index: 1;">
                <div class="navbar-nav w-100 overflow-hidden" style="height: 299px">
                    @await Component.InvokeAsync("CategoryMenu")
                </div>
            </nav>
        </div>
        <div class="col-lg-9">
            <nav class="navbar navbar-expand-lg bg-light navbar-light py-3 py-lg-0 px-0">
                <a href="@Url.Action("Index", "Home")" class="text-decoration-none d-block d-lg-none">
                    <h1 class="m-0 display-5 font-weight-semi-bold"><span class="text-primary font-weight-bold border px-3 mr-1">E</span>Shopper</h1>
                </a>
                <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse justify-content-between" id="navbarCollapse">
                    <div class="navbar-nav mr-auto py-0">
                        <a href="@Url.Action("Index", "Home")" class="nav-item nav-link @(ViewContext.RouteData.Values["Action"].ToString() == "Index" && ViewContext.RouteData.Values["Controller"].ToString() == "Home" ? "active" : "")">Home</a>
                        <a href="@Url.Action("Index", "Product")" class="nav-item nav-link @(ViewContext.RouteData.Values["Controller"].ToString() == "Product" ? "active" : "")">Shop</a>
                        <div class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">Pages</a>
                            <div class="dropdown-menu rounded-0 m-0">
                                <a href="/Cart/Cart" class="dropdown-item">Shopping Cart</a>
                                <a href="/Order/Checkout" class="dropdown-item">Checkout</a>
                            </div>
                        </div>
                        <a href="@Url.Action("Privacy", "Home")" class="nav-item nav-link @(ViewContext.RouteData.Values["Action"].ToString() == "Privacy" ? "active" : "")">Privacy</a>
                        <a href="@Url.Action("Contact", "Home")" class="nav-item nav-link @(ViewContext.RouteData.Values["Action"].ToString() == "Contact" ? "active" : "")">Contact</a>
                    </div>
                    <div class="navbar-nav ml-auto py-0">
                        @if (User.Identity.IsAuthenticated)
                        {
                            <div class="nav-item dropdown">
                                <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">
                                    Welcome, @User.Identity.Name
                                </a>
                                <div class="dropdown-menu rounded-0 m-0">
                                    @{
                                        var roleId = User.Claims.FirstOrDefault(c => c.Type == "RoleId")?.Value;
                                        if (roleId == "1") // ADMIN
                                        {
                                            <a href="@Url.Action("Index", "AdminDashboard")" class="dropdown-item">Admin Dashboard</a>
                                        }
                                        else if (roleId == "2") // VENDOR
                                        {
                                            <a href="@Url.Action("Index", "Vendor")" class="dropdown-item">Vendor Dashboard</a>
                                        }
                                    }
                                    <a href="/api/User/Profile" class="dropdown-item">Profile</a>
                                    <a href="/Order/MyOrders" class="nav-item nav-link">My Orders</a>
                                    <form asp-action="Logout" asp-controller="Auth" method="post" style="margin: 0;">
                                        @Html.AntiForgeryToken()
                                        <button type="submit" class="dropdown-item">Logout</button>
                                    </form>
                                </div>
                            </div>
                        }
                        else
                        {
                            <a href="@Url.Action("Login", "Auth")" class="nav-item nav-link">Login</a>
                            <a href="@Url.Action("Register", "Auth")" class="nav-item nav-link">Register</a>
                        }
                    </div>
                </div>
            </nav>
        </div>
    </div>
</div>
<!-- Navbar End -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<!-- Sau đó là Popper.js (cần thiết cho Bootstrap) -->
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>

<!-- Cuối cùng là Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/js/bootstrap.min.js"></script>
<script>
$(document).ready(function() {
    // Khởi tạo tất cả dropdown
    $('.dropdown-toggle').dropdown();
    
    // Debug - kiểm tra xem dropdown có được click không
    $('.dropdown-toggle').on('click', function(e) {
        console.log('Dropdown clicked');
    });
});
</script>
